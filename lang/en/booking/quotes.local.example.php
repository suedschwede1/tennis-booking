<?php

declare(strict_types=1);

// Copy this file to lang/en/booking/quotes.local.php (not tracked in git).
// When the file exists and returns a non-empty list, it fully replaces the
// base quote pool — the built-in quotes from lang/en/booking/public.php
// ('quotes') are no longer used. ':name' is replaced at runtime with the
// booking member's display name.

return [
    // '🎾 :name, have fun on the court!',
    // '🏆 The court is all yours today, :name!',
];
